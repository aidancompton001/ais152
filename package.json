{
  "name": "ais152-quiz",
  "version": "1.0.0",
  "description": "AiS152 quiz logic — pure functions tested with node:test",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.js"
  }
}
