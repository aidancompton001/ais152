# PX-022 — Imkerei Staskiewitz «The Crazy Bee» в портфолио, первой позицией (15 → 16)

**Дата:** 2026-09-10
**Статус:** исполнение (прямое указание CEO голосом)
**Ответственный:** #1 Product Architect
**Размер:** M
**Шаблон:** [PX-015 ElektroCheck](PX-015_elektrocheck_portfolio_entry.md) + `data/README.md`

---

## Задача CEO (дословно по смыслу)

Добавить проект с пчёлами в портфолио на ais152.com, скриншотом, **первой
позицией**. Обновить все цифры и номера по количеству проектов. Ничего не сломать.

## Проект

- Клиент: Imkerei Staskiewitz «The Crazy Bee», Waldaschaff, Naturpark Spessart
- Живой сайт: https://thecrazybee.de — 200, открыт для посетителей
- Работа: перенос дизайна в WordPress, 26 контентных страниц со старого сайта,
  WooCommerce-магазин, 203 редиректа со старых адресов

## Анализ последствий

**Создаётся:**
- `assets/staskiewitz-site.jpg` — снимок главной живого сайта, 2880×1800, как у
  соседних feature-карточек
- `docs/tasks/PX-022_…md` — этот файл
- `verify/acceptance.staskiewitz.json` — приёмка этой задачи. Основной
  `verify/acceptance.json` занят задачей AIS152-REVIEWS-001 и не трогается

**Модифицируется:**
- `data/projects.json` — новая запись `order:1, featured:true, layout:"feature"`;
  все 15 существующих сдвигаются `order +1`, их взаимный порядок сохраняется
- `_src/index.src.html` — SVG-знак `mark-staskiewitz` (пчела) в спрайт;
  счётчик статистики `data-target="16"` заменяется на `{{N}}`
- `_src/leistungen_index.html` + `scripts/build_services.py` — «Sechzehn»
  заменяется подстановкой из данных
- Собранные страницы — только сборкой: `index.html`, `en/index.html`,
  `leistungen/*.html`, `projekte/*.html`, `sitemap.xml`

**Не трогается:**
- Верстка карточек, стили, скрипты, квиз
- Порядок существующих 15 проектов между собой
- `verify/acceptance.json`, `DEVLOG.md` и `verify/hrc.py` — в них чужие
  незакоммиченные правки из других задач, в коммит не входят

## Два числа уже сейчас врут

При осмотре найдено: число проектов почти везде считается при сборке из
`data/projects.json`, но в двух местах стоит руками:

| Где | Сейчас | Живых проектов | Верно? |
|---|---|---|---|
| Счётчик «Production projects» на главной | `data-target="16"` | 15 | нет |
| Хаб /leistungen/, последняя строка | «Sechzehn» | 15 | нет |

После добавления оба совпадут с 16 — случайно. Совпадение, которое держится
на памяти, разъедется при следующем проекте. Поэтому оба места переводятся
на подстановку из данных, как остальные семь.

## Критерий готовности

На живом ais152.com карточка The Crazy Bee стоит первой (`01 / 16`), ведёт на
https://thecrazybee.de, скриншот грузится, число 16 стоит во всех местах на
обоих языках, старых 15 не осталось, остальные 15 карточек на месте в прежнем
порядке, страница проекта `/projekte/staskiewitz.html` собрана.

## Порядок

1. Тег отката `v2-pre-px022`
2. Снимок живого сайта → `assets/staskiewitz-site.jpg`
3. Запись в `data/projects.json`, сдвиг order
4. Знак-пчела, два захардкоженных числа → подстановка
5. Сборка всеми четырьмя скриптами
6. Приёмка `verify/acceptance.staskiewitz.json` — 100%
7. Коммит только своих файлов, пуш, проверка живого сайта
